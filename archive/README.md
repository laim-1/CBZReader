# Archive

`full-reader/` is the previous CBZ reader UI (Vite, toolbar, magnifier, etc.).

To restore it later, move its files back to the repo root and run `npm install` / `npm run build` from that folder.
